# Логотип слева на экране года

## Что изменено

- На `YearHomeScreen` мини-логотип перенесён в левую колонку шапки; справа остаётся только кнопка настроек.
- Колонки слева и справа по-прежнему одинаковой ширины (32 + 8 + 36), заголовок года с `flex: 1` и `textAlign: 'center'` — год визуально по центру.

## Что проверено

- `npm test -- --testPathPattern=App.test` — успешно.
