# IDN: изображение 18–24 марта (Nyepi + Idul Fitri)

## Что изменено

- Ассет заменён на `assets/days_img/id_18-24march.webp` (константа `idNyepiEidFitrMarchClusterImage`).
- Для дат **2026-03-18 … 2026-03-24** добавлены дублирующие ключи с текстами из `calendar2026IDN.json` (`name_ru` / `name_id`), чтобы резолв работал и при опоре только на индонезийские поля.

## Что проверено

- `npx jest __tests__/holidayImages.test.ts`.

## Дальнейшие шаги

- Файл `id_18-24mar.webp` в репозитории не удалялся — при желании убрать дубликат вручную, если он больше не нужен в дизайне.
