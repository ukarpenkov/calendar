# Иконка настроек (SVG)

## Что изменено

- Вместо символа «⚙» в `SettingsGearButton` используется векторная иконка `SettingsGearIcon` на базе `react-native-svg` (контурная шестерёнка в духе Feather, согласованная по толщине линии с `NavigationIcons`).
- Добавлен файл `src/shared/ui/icons/SettingsGearIcon.tsx`.

## Что проверено

- `npx tsc --noEmit` — без ошибок.
- Линтер для изменённых файлов — замечаний нет.

## Дальнейшие шаги

- При необходимости подогнать `strokeWidth` / `size` под визуальную плотность после просмотра на реальном устройстве.
