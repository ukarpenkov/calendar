# Удаление героя на экране года

## Что изменено

- На `YearHomeScreen` убран блок под шапкой: иконка приложения (`AppLogo`), заголовок «Production calendar» / «Производственный календарь» и подзаголовок про SQLite.
- Из `i18n` удалены ключи `year.header.eyebrow` и `year.header.subtitle` как неиспользуемые.
- В `__tests__/App.test.tsx` проверка успешного показа года после bootstrap заменена на наличие месяца «January» вместо удалённых строк.

## Что проверено

- `npm test -- --testPathPattern=App.test` — все 9 тестов проходят.

## Дальнейшие шаги

- При необходимости можно вернуть краткий подзаголовок в другом месте (например, только в настройках).
