# Уменьшен зазор вокруг блока «Выбранный день»

## Что изменено

- В `src/pages/month/ui/MonthDetailScreen.tsx` уменьшен `gap` у `monthContentColumn` с общего большого межсекционного значения до `MONTH_TOTALS_GAP`.
- В вертикальной раскладке промежутки над и под блоком «Выбранный день» теперь ближе к расстояниям между карточками статистики ниже.

## Что проверено

- Проверен изменённый файл `src/pages/month/ui/MonthDetailScreen.tsx` через встроенный lint: новых ошибок нет.
- Подтверждено по коду, что изменение затрагивает только stack-раскладку экрана месяца.

## Дальнейшие шаги

- Если нужно, можно ещё на 1-2 px подстроить зазор после визуальной проверки на устройстве.
