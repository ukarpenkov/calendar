# Pencil: выравнивание варианта brand • light

## Что изменено

- В `pencil-new.pen` у фрейма `kAft8` («palette: brand • light») у экземпляра компонента `kCRJe` (`hfLy3`) удалено переопределение `descendants` для узла `qVS1J`, чтобы иконка отображалась так же, как в соседних светлых палитрах **forest**, **sunset** и **mono** (без локальной подгонки текста/глифа).

## Что проверено

- Файл `pencil-new.pen` успешно парсится как JSON после правки.

## Дальнейшие шаги

- Открыть макет в Pencil и визуально сверить превью с палитрами forest/sunset/mono.
- Pencil MCP при попытке открытия этого файла вернул таймаут; при необходимости повторить правки через MCP при открытом документе в редакторе.
