# Префикс «Год» / «Year» в заголовке экрана года

## Что изменено

- Добавлен ключ локализации `year.home.title`: EN `Year {{year}}`, RU `Год {{year}}`.
- В `YearHomeScreen` вместо голого числа в центре шапки выводится строка через `t('year.home.title', { year: calendar.year })`.
- Тесты: в `App.test` проверка года заменена на `Year 2026`; в `localization.test` добавлены проверки интерполяции.

## Что проверено

- `npm test` — все тесты проходят.
