# Отступы блока «Выбранный день» в вертикальном режиме

## Что изменено

- В `src/pages/month/ui/MonthDetailScreen.tsx` для контейнера `monthContentColumn` добавлен `gap: layout.contentStackGap`.
- За счёт этого в stack-раскладке появился одинаковый внешний вертикальный отступ между календарём, блоком «Выбранный день» и карточками итогов.
- Split-раскладка не менялась, потому что там этот зазор уже задаётся через `monthSideColumn`.

## Что проверено

- Проверен изменённый файл `src/pages/month/ui/MonthDetailScreen.tsx` через встроенный lint: новых ошибок нет.
- Подтверждено по коду, что изменение влияет именно на вертикальную компоновку экрана месяца.

## Дальнейшие шаги

- При желании можно быстро сверить экран в эмуляторе или на устройстве, чтобы визуально подтвердить одинаковый ритм между секциями.
