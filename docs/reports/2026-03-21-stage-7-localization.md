# Отчет: Stage 7 Localization

## Что изменено

- Добавлен глобальный `AppLocalizationProvider` с определением стартового языка устройства и runtime-переключением `ru/en`.
- Создан общий слой `i18n` для переводов интерфейса, локализованных названий месяцев, сокращений дней недели и подписей типов дней.
- На экран `Settings` добавлен переключатель языка, а `Splash`, `Year`, `Month detail`, `Settings` и error-состояния переведены на общий словарь.
- Локализация протянута в calendar presentation helpers, чтобы месяцы и day-type labels формировались из единого источника.
- Добавлены и обновлены тесты для локализации и локализованных calendar summaries/details.

## Что проверено

- Выполнен `npm test -- --runInBand`: `6/6` test suites passed.
- Выполнен `npm run lint`: без ошибок.
- Проверены IDE lints по затронутым файлам: ошибок нет.

## Дальше по плану

- При необходимости сохранить выбранный язык между перезапусками через локальное хранилище настроек.
- Следующим подпунктом этапа `Settings` подключить `JSON import entry`.
