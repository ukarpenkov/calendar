# Исправление двухколоночной сетки на экране года

## Что изменено

- На экране года в `src/pages/year/ui/YearHomeScreen.tsx` убрана раскладка через `flexWrap` и процентную ширину карточек месяцев.
- Сетка месяцев переведена на детерминированные строки по две карточки, чтобы экран всегда строился в два столбца независимо от особенностей конкретного Android-устройства.
- Добавлен документ для закрытого тестирования `docs/closed-testing/2026-04-06-year-screen-two-columns.md` с описанием критичного бага, затронутых устройств и ожидаемого поведения.
- Заодно очищен локальный `eslint` warning в обработчике открытия Telegram-ссылки на том же экране.

## Что проверено

- Выполнен `npx eslint "src/pages/year/ui/YearHomeScreen.tsx"`: ошибок и предупреждений нет.
- Выполнен `npm test -- --runInBand`: успешно, `16/16` наборов тестов и `73/73` тестов.
- Проверены IDE-диагностики для изменённого файла: ошибок нет.

## Дальнейшие шаги

- Проверить экран года на Honor 200 и Realme GT7T после сборки, чтобы подтвердить стабильные 2 столбца в реальном окружении.
- При необходимости добавить скриншоты «до/после» в материалы закрытого тестирования.
