# Отчет: Global plan stage 8 import picker and validation

## Что изменено

- В `docs/GLOBAL-DEVELOPMENT-PLAN.md` добавлен новый `8` пункт вместо прежнего этапа про polish.
- Новый этап фиксирует ближайший приоритет: подключить `file picker` к импорту `JSON`, завершить валидацию и вручную проверить сценарий загрузки календаря `2025` года.
- Последующие этапы глобального плана сдвинуты на один номер вниз, чтобы сохранить последовательность дорожной карты.

## Что проверено

- Проверен обновленный порядок пунктов в секции поэтапного плана: нумерация идет последовательно от `1` до `13`.
- Проверен текст нового `8` пункта: он явно описывает `file picker`, доработку валидации и ручной тест на импорте `2025` года.

## Что дальше

- Следующей практической задачей можно брать реализацию выбора локального файла и проброса содержимого в существующий import flow.
- После этого стоит отдельно прогнать реальный `2025` `JSON` через тот же путь валидации и замены данных, который будет использоваться в приложении.
